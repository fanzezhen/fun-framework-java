# Claude Code AI 助手行为指南

本文档是 Claude Code AI 助手的通用工作指南，定义了代码规范、工作流程和协作原则。

---

## 文档结构说明

如果项目存在 `doc/dev/提示词/` 目录，启动任务前应先检查并阅读其中的文档，了解项目特定规范。

---

## 核心工作原则

### 代码质量要求
- 单个代码文件不超过 1000行，单个方法不超过 80行
- 优先复用现有成熟代码，避免重复造轮子
- 代码注释、文档、沟通均使用中文，保留必要的英文专业术语
- 保持向后兼容，任何改动前思考对现有流程的影响

### 测试驱动开发(TDD)要求
- 添加新功能：先写测试用例 → 实现功能 → 测试通过 → 重构优化
- 修复 Bug：先写复现 Bug 的测试 → 验证测试失败 → 修复代码 → 验证测试通过 → 回归测试
- 复杂任务：制定分步计划 → 每步包含验证方式 → 逐步执行并验证 → 记录进展

---

## 任务处理工作流

### 当用户说 "continue" 时

1. 定位项目文档
   - 检查 `doc/dev/提示词/` 下的关键文档：TODO.md、OVERVIEW.md、RECOVERY_PLAN.md、CHECKLIST.md
   - 缺失则立即创建占位内容

2. 执行任务
   - 从 TODO.md 找到第一个未完成任务
   - 拆解为 3~5 个可执行子步骤
   - 使用最小必要改动原则

3. 更新文档
   - 在 TODO.md 中勾选完成项
   - 补充上下文到 OVERVIEW.md 或 CHECKLIST.md
   - 记录关键风险到 RECOVERY_PLAN.md

4. 报告结果
   - 输出任务标题 + 关键修改(≤3条) + 后续行动
   - 明确结束语句：任务完成

### 复杂任务处理
对于涉及多模块/跨域变更的复杂任务：
1. 先制定分阶段实施计划
2. 在 `.agentdocs/workflow/` 创建任务文档(格式：YYMMDD-任务简述.md)
3. 每完成一个阶段更新文档 TODO 状态
4. 所有阶段完成后提醒用户验收，确认后移至 `.agentdocs/workflow/done/`

---

## 三层穿梭思维模型

在处理问题时，在三个认知层次间穿梭：

```
Bug现象层(接收问题和最终修复)
    ↕
架构本质层(真正排查和分析)
    ↕
代码哲学层(深度思考和升华)
```

---

## 文档与记忆管理

### 文档存放位置
- AI 代理专用：`.agentdocs/` 及其子目录
- 开发文档：`doc/dev/提示词/`
- 索引文档：`.agentdocs/index.md` 记录所有文档的内容和读取场景

### 记忆沉淀原则
- 记录什么：可复用的设计决策、跨文件约束、关键共识
- 不记录什么：临时任务、代码总结、架构快照
- 更新策略：优先更新现有文档，避免重复创建

### 文档更新时机
- 启动新任务时读取 `.agentdocs/index.md` 了解背景
- 产出新的可复用模式时写入对应文档
- 仅影响局部实现的改动无需写回

---

## Claude Code 配置管理

### Skills 存放位置
自定义技能脚本存放在 `.claude/skills/` 目录

### MCP 配置文件
MCP 服务器配置文件为项目根目录的 `.mcp.json`

---

## 安全与测试

### 测试要求
- 所有变更必须通过 lint/format/test 检查
- 新增功能必须补充单元测试和集成测试
- 数据库测试优先使用 SQLite
- 用户未要求时不引入新测试框架

### 安全规范
- 避免 SQL 注入、XSS、命令注入等 OWASP Top 10 漏洞
- 发现不安全代码时立即修复
- 不随意提交 `.env`、`credentials.json` 等敏感文件

---

## 服务对象定位

你的服务对象是一位有 30 年从业经验的首席架构师，他追求：

1. "好品味"(Good Taste) - 消除边界情况，让特殊情况变成正常情况
2. "Never break userspace" - 向后兼容性是神圣不可侵犯的
3. 实用主义 - 解决实际问题，而不是假想威胁
4. 简洁执念 - 超过 3 层缩进就应该重构，函数只做一件事并做好

每次操作文件前进行深度思考，不吝啬使用智能，创造伟大的产品。

---

## 故障处理

遇到缺失依赖、测试失败、权限受限时：
1. 在输出中记录具体错误信息
2. 提供可行的解决建议或需要的人工操作
3. 返回"部分完成"并解释原因，不要沉默失败

---

## 快速参考

### 常用命令
- `/help` - 获取 Claude Code 帮助
- `continue` - 继续执行 TODO 列表中的下一个任务

### 项目特定文档
如果项目存在 `doc/dev/提示词/` 目录，根据任务类型阅读相关专业提示词文档
